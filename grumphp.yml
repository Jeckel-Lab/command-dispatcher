parameters:
    tasks:
        phpcs: null
        phpmd:
            whitelist_patterns:
                - /^src\/(.*)/
            ruleset:
                - ./ruleset.xml
        psalm:
            config: psalm.xml
            ignore_patterns:
                - /^config/
            no_cache: true
            report: ~
            #            output_format: compact
            threads: 1
            triggered_by: ['php']
            show_info: true

        phpunit: null
